---
description: Sakrylle 部署流程、服务器布局与配置热重载
globs: config/**,docker-compose*.yaml,.github/**
alwaysApply: false
---

# 部署与服务器

## 代码改动 — GHA → GHCR → 拉镜像

```bash
git push origin theme/sakrylle
gh run list -R Ranshen1209/sakrylle-status --limit=1   # 等 ~3 分钟
ssh ssh-tokyo 'cd /opt/stack && docker compose pull relay-pulse && docker compose up -d --force-recreate relay-pulse'
curl -sI https://status.sakrylle.com/health
```

## 仅 config.yaml — 热更新

`config.yaml` 走 fsnotify（`internal/config/watcher.go`）：

```bash
scp config/config.yaml ssh-tokyo:/opt/stack/relay-pulse/config/config.yaml
ssh ssh-tokyo 'docker logs --tail=20 relay-pulse 2>&1 | grep -i reload'
```

`.env` **不**热更新，改后必须 `docker compose up -d --force-recreate relay-pulse`。

## 服务器布局

```
/opt/stack/
├── docker-compose.yml
└── relay-pulse/
    ├── .env                    # API keys (mode 600)
    ├── config/
    │   ├── config.yaml
    │   └── templates/
    └── stack_relay-pulse-data → /data/monitor.db
```

反代: `443 → sslh → 127.0.0.1:8443 → Nginx (status.sakrylle.com) → relay-pulse:8080`
Nginx: `/opt/stack/nginx/conf.d/sakrylle-status.conf`，网络 `stack_default`。

## API keys

- 路径: `/opt/stack/relay-pulse/.env`，compose `env_file:` 注入
- `config.yaml` 用 `env_var_name:` 显式引用，覆盖自动名 `MONITOR_{PROVIDER}_{SERVICE}_{CHANNEL}_API_KEY`
- sub2api 后台命名 `relay-pulse-{channel}`，env 约定 `MONITOR_SAKRYLLE_<CHANNEL>_API_KEY`
- 轮换后必须 force-recreate；孤立 env 行无害
