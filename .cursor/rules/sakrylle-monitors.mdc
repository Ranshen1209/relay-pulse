---
description: Sakrylle 生产探针配置、重试策略与踩坑
globs: config/**
alwaysApply: false
---

# Monitors（生产）

7 探针，`interval: 3m`，打 `https://api.sakrylle.com`，**每 group 独立 API key**（sub2api `api_keys.group_id` 单值）。

| Channel | Service | Template | Model | Group | Rate |
|---|---|---|---|---|---|
| `claude-kiro-special` | `cc` | `cc-haiku-openai-chat` (fork) | `claude-haiku-4-5-20251001` | Claude-Kiro-Special (2) | 0.5x |
| `claude-code-awsq` | `cc` | `cc-haiku-openai-chat` | `claude-haiku-4-5-20251001` | Claude-Code-AWSQ (12) | 0.4x |
| `gpt-pro` | `cx` | `cx-gpt-mini-chat` | `gpt-5.4-mini` | GPT-Pro (14) | 0.5x |
| `gpt-pro-special` | `cx` | `cx-gpt-mini-chat` | `gpt-5.4-mini` | GPT-Pro-Special (3) | 0.4x |
| `gpt-plus` | `cx` | `cx-gpt-mini-chat` | `gpt-5.4-mini` | GPT-Plus (4) | 0.25x |
| `deepseek` | `dx` | `dx-flash-openai-chat` | `deepseek-v4-flash` | Deepseek (6) | 0.7x |
| `deepseek-official` | `dx` | `dx-flash-openai-chat` | `deepseek-v4-flash` | Deepseek-Official (9) | 1.0x |

不监测: GPT-Image (5)、GPT-Image-2-4K (11)。已下架: Claude-Code (7)、Claude-Special (8)、GPT-Plus-Special (10)。

成本约 $0.02/天（3m + haiku/mini）。2026-06-04: 旧 GPT-Pro (3)→GPT-Pro-Special，新 GPT-Pro (14)；`gpt-pro` 历史迁至 `gpt-pro-special`。

## Retry / timeout

- monitor: `retry: 3`（4 次）+ `timeout: 30s`；优先级 monitor > template > global
- `internal/monitor/probe.go`: **超时不重试**，timeout 为所有尝试总预算
- 退避: `retry_base_delay=200ms`, `retry_max_delay=2s`, `retry_jitter=0.2`

## 踩坑

**claude-kiro-special**: 必须用 `cc-haiku-openai-chat`，禁用原生 `cc-haiku-arith`（200 ~15ms 但 `usage_logs` 空 → 静默假阳性）。

**Deepseek**: OpenAI 路径 + `max_tokens: 8`（抑制 thinking 成本）；只探 `v4-flash`。

**GPT-Image**: 已下线（按次 $0.15，探针太贵）。
